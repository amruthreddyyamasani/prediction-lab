import {Flame, Gauge, Layers3, Plus, Sparkles, Zap} from "lucide-react"; import {reactions} from "../data/reactions";
const icons=[Sparkles,Zap,Layers3,Flame,Gauge]; type Props={active:string;onSelect:(id:string)=>void};
export function ExperimentRail({active,onSelect}:Props){return <section className="experiment-rail panel"><div className="panel-head compact"><div><span className="eyebrow">PROTOCOL LIBRARY</span><h2>Choose an experiment</h2></div><span className="protocol-count">05 MODELS</span></div><div className="experiment-list">{reactions.map((r,i)=>{const Icon=icons[i];return <button className={`experiment ${active===r.id?"active":""}`} key={r.id} onClick={()=>onSelect(r.id)}><div className="exp-icon"><Icon size={16}/></div><div><strong>{r.shortName}</strong><span>{r.reactants.join(" + ")}</span></div><i>↗</i></button>})}<button className="experiment free"><div className="exp-icon"><Plus size={16}/></div><div><strong>Free exploration</strong><span>Build your own mixture</span></div></button></div></section>}
