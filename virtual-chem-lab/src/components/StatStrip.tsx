type Props={ph:number;temperature:number;volume:number;progress:number;activity:string};
export function StatStrip({ph,temperature,volume,progress,activity}:Props){return <div className="stat-strip"><div><span>pH</span><strong>{ph.toFixed(2)}</strong><small>{ph<6?"acidic":ph>8?"basic":"near neutral"}</small></div><div><span>TEMPERATURE</span><strong>{temperature.toFixed(1)}°C</strong><small>thermal probe</small></div><div><span>VOLUME</span><strong>{volume.toFixed(1)} mL</strong><small>combined</small></div><div className="activity"><span>REACTION PROGRESS</span><strong>{progress}%</strong><small>{activity}</small></div></div>}
