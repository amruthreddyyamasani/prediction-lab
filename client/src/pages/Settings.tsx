import { Database, LogOut, Moon, ShieldCheck, Sun, UserRound } from "lucide-react";
import { Link } from "wouter";
import { useTheme } from "@/contexts/ThemeContext";
import { useSupabaseAuth } from "@/hooks/useSupabaseAuth";

export default function Settings() {
  const { user, signOut } = useSupabaseAuth();
  const { theme, toggleTheme } = useTheme();
  if (!user) return <div className="page-empty"><div className="empty-index">05 / settings</div><h1>Your account,<br /><em>your settings.</em></h1><p>Sign in to manage your account and display preferences.</p><Link className="primary-button" href="/">Return home</Link></div>;
  return <div className="internal-page settings-page"><div className="page-heading"><div><div className="eyebrow"><span className="eyebrow-line"></span> Account and display</div><h1>Settings</h1><p>Manage your account and display preferences.</p></div></div><div className="settings-list"><section className="setting-row"><div className="setting-icon"><UserRound size={18} /></div><div className="setting-copy"><h2>Research identity</h2><p>{user.email}</p></div><span className="setting-value">Supabase Auth</span></section><section className="setting-row"><div className="setting-icon"><Sun size={18} /></div><div className="setting-copy"><h2>Interface theme</h2><p>Choose the interface theme used across Prediction Lab.</p></div><button className="theme-toggle" onClick={toggleTheme}><span className={theme === "dark" ? "selected" : ""}><Moon size={14} /> Dark</span><span className={theme === "light" ? "selected" : ""}><Sun size={14} /> Light</span></button></section><section className="setting-row"><div className="setting-icon"><Database size={18} /></div><div className="setting-copy"><h2>Data model</h2><p>Predictions, forecast versions, evidence, and resolutions live in relational Supabase tables with row-level security.</p></div><span className="setting-value secure"><ShieldCheck size={14} /> RLS enabled</span></section><section className="setting-row danger-row"><div className="setting-icon"><LogOut size={18} /></div><div className="setting-copy"><h2>End session</h2><p>Sign out of this browser. Your saved record remains in Supabase.</p></div><button className="text-button" onClick={() => signOut()}>Sign out</button></section></div></div>;
}
